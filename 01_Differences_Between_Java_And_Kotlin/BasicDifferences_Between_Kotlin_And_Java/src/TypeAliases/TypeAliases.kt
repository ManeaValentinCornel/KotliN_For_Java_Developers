package TypeAliases
typealias EmployeeSet =Set<Employee>


//StringBuilder is actually a type alias in Kotlin
//Kotlin doesnt have its own StringBuilderClass but uses typealias to it
fun main(){
    var employees: EmployeeSet
}
class Employee(){
}

